import { useNavigate } from "react-router-dom";

function AddAccount() {
    const navigate = useNavigate();

    return (
        <div>
            <h1>add</h1>

        </div>
    );
}

export default AddAccount;
